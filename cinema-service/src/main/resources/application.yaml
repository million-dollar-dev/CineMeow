server:
  port: 8084
  servlet:
    context-path: /cinema

spring:
  application:
    name: cinema-service
  datasource:
    url: jdbc:postgresql://localhost:5432/cinemeow_cinema
    username: postgres
    password: sa123
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  cache:
    type: redis
    redis:
      time-to-live: 3600000
      cache-null-values: false
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 2
          max-wait: -1ms
        shutdown-timeout: 100ms
      connect-timeout: 2000ms
      timeout: 1000ms

springdoc:
  api-docs:
    enabled: true
    path: /v1/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui
    operations-sorter: method

openapi:
  service:
    api-docs: cinema-service
    server: http://localhost:8084/cinema
    title: Cinema Service
    version: 1.0.0